name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [18, 20, 22]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Syntax check
        run: |
          node --check bin/cli.js
          node --check lib/render.js
          node --check lib/init.js

      - name: Render smoke test
        run: |
          RESET=$(( $(date +%s) + 15240 ))
          JSON="{\"rate_limits\":{\"five_hour\":{\"used_percentage\":39,\"resets_at\":$RESET},\"seven_day\":{\"used_percentage\":19}}}"
          OUT=$(printf '%s' "$JSON" | CC_STATUSLINE_LANG=en node bin/cli.js render)
          echo "Output: $OUT"
          echo "$OUT" | grep -q "5H"
          echo "$OUT" | grep -q "39%"
          echo "$OUT" | grep -q "7D"
          echo "$OUT" | grep -q "19%"
          echo "$OUT" | grep -qE '\([0-9]+h[0-9]+m\)'

      - name: Session-token segment smoke test
        run: |
          mkdir -p ~/.cache/cc-statusline
          printf '17058499' > ~/.cache/cc-statusline/sesstokens-ci.txt
          printf '19852118' > ~/.cache/cc-statusline/sesstokens-ci2.txt
          render() {
            printf '{"session_id":"%s","rate_limits":{"five_hour":{"used_percentage":39},"seven_day":{"used_percentage":19}}}' "$1" | CC_STATUSLINE_LANG=en node bin/cli.js render
          }
          OUT=$(render ci); echo "Output: $OUT"
          echo "$OUT" | grep -q "🪙 17.1M"
          # Two sessions must report their own totals, not a shared one.
          OUT2=$(render ci2); echo "Other session: $OUT2"
          echo "$OUT2" | grep -q "🪙 19.9M"
          # The opt-out must suppress the segment even with a fresh cache present.
          OFF=$(printf '{"session_id":"ci"}' | CC_STATUSLINE_LANG=en CC_STATUSLINE_NO_SESSION_TOKENS=1 node bin/cli.js render)
          echo "Opt-out: $OFF"
          ! echo "$OFF" | grep -q "🪙"

      - name: Unit tests
        run: npm test

      - name: Init smoke test
        run: |
          TMP=$(mktemp -d)
          CLAUDE_CONFIG_DIR="$TMP" node bin/cli.js init
          grep -q '"statusLine"' "$TMP/settings.json"
          echo "init wrote statusLine OK"

      - name: Help and version
        run: |
          node bin/cli.js --version
          node bin/cli.js --help | head -1
